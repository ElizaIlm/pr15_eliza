<Page x:Class="Documents_ilmasheva.Pages.Main"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Documents_ilmasheva.Pages"
      mc:Ignorable="d" 
       d:DesignHeight="450" d:DesignWidth="800"
      Title="Main">

    <Grid>
      
            <!-- Заголовок -->
            <Border Background="#FF808080" Padding="10,5" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <Image Source="/Image/icon-black.png" Width="24" Height="24" Margin="0,0,10,0"/>
                    <TextBlock Text="Учёт входящих и исходящих документов" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Foreground="White"/>
                <Button Content="Выйти" Click="Exit" Margin="320,0,0,0" Width="66"/>
            </StackPanel>
            </Border>

            <!-- Основное содержимое -->
            <ScrollViewer Margin="0,50,0,50" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="parent" Margin="10"/>
            </ScrollViewer>

            <!-- Кнопка Добавить внизу -->
            <Button Content="Добавить" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10" Click="AddDocuments"/>
       
    </Grid>
</Page>
